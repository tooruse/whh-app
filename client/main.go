package main

import (
  "fmt"
)


func main(){
  p := 8000
  connect(p)
}


func connect(p int){
  for {
        // do something
    if !condition {      // the condition stops matching
          //send init data to server
          break        // break out of the loop
    }
  }
}
